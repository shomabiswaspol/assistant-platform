# Secrets — never commit
.env
backend/.env
omniroute/.env
*.env.local

# Dependencies / build output
node_modules/
frontend/dist/
frontend/node_modules/
backend/node_modules/

# Local-only tooling (installed CLI, not project source)
.omniroute-cli/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
