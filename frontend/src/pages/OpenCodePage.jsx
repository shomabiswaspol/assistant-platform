export default function OpenCodePage() {
  return (
    <div className="page">
      <h1>OpenCode</h1>
      <p>
        Not implemented in this pass — a real OpenCode integration needs a decision on how it talks to
        this platform (its own CLI session vs. an API bridge) before building a UI around it.
        See the deployment report's next-implementation order.
      </p>
    </div>
  );
}
